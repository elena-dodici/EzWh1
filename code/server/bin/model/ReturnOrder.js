'use strict'

class ReturnOrder {
    constructor (id, returnDate, relativeRestockOrder) {
        this.id = id;
        this.returnDate = returnDate;
        this.relativeRestockOrder = relativeRestockOrder;
    }
}